# Check Brackets

- create a stack data structure
- loop through each character in the input string
- if current char is open bracket, push to stack
- if current char is close bracket
  - check if no open bracket element in the stack
    - if true, return mismatch position
  - check if the top element in the open brackets stack if pair with this current close bracket
    - if true, we found the match so remove the top element stack using pop
    - if false, return mismatch position
- check if after looping through all characters, there are still open brackets in the stack
  - if true return that element position as mismatch
  - else return -1 indicate no mismatch found
